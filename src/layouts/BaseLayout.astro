---
import MainHead from "../components/MainHead.astro";

interface Props {
	title?: string;
	lang?: string;
}

const { title, lang = 'en' } = Astro.props;
---

<html lang={lang}>
	<head>
		<MainHead title={title} />
	</head>
	<body>
		<slot />
	</body>
</html>
